<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.SwiftWords" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_screen_background</item>
        <item name="windowActionBar">false</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_screen_pic</item>
        <item name="postSplashScreenTheme">@style/Theme.SwiftWords</item>
    </style>
</resources>